specVersion: "v1beta"
serviceId: "blog-module"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-module-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
